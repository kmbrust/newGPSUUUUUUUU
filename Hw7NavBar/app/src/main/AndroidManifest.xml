<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kmbru_000.hw7navbar" >

    <uses-permission android:name="android.permission.READ_CONTACTS" >
    </uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Drawer_Data"
            android:label="@string/title_activity_drawer__data" >
        </activity>
        <activity
            android:name=".DatePicker"
            android:label="@string/title_activity_date_picker" >
        </activity>
        <activity
            android:name=".ContactApp"
            android:label="@string/title_activity_contact_app" >
        </activity>
    </application>

</manifest>
